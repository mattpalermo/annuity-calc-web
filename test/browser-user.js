/* eslint-env mocha */
/* eslint no-unused-vars: ["error", { "varsIgnorePattern": "should" }]*/
const should = require("chai").should();

describe("browser user acceptance tests", function() {
	describe("use calculator", function() {
		it("Use selenium to play out a user story");
	});
});
